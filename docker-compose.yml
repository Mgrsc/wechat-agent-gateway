services:
  wechat-agent-gateway:
    image: ghcr.io/mgrsc/wechat-agent-gateway:latest
    env_file:
      - .env.docker
    ports:
      - "8080:8080"
    volumes:
      - wechat-agent-gateway-data:/data
    restart: unless-stopped

volumes:
  wechat-agent-gateway-data:
